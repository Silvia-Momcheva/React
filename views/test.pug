doctype html
html
  head
    title Test with flashcards
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css")
    script(src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js")
    script(src="https://www.gstatic.com/firebasejs/8.10.1/firebase-auth.js")
    script(src="https://www.gstatic.com/firebasejs/8.10.1/firebase-firestore.js")
    script(src="/scripts/firebase.js")
    script(src="/scripts/test.js")

  body(class="bg-gradient-to-r from-purple-500 via-pink-500 to-red-500 min-h-screen p-6 flex flex-col items-center justify-start")

    // Back button
    a(href="/home", class="absolute top-4 left-4 text-white hover:text-yellow-200 flex items-center text-lg")
      svg(xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke="currentColor", class="w-6 h-6 mr-1")
        path(stroke-linecap="round", stroke-linejoin="round", stroke-width="2", d="M15 19l-7-7 7-7")
      | Back

    h1.text-3xl.font-bold.text-white.mb-6 Test with flashcards

    // Select dropdown
    select#setsSelect(class="mb-6 p-3 rounded-lg text-gray-800")
      option(value="") -- Select set --

    // Progress bar
    .w-full.max-w-xl.mb-4
      .flex.justify-between.text-white.text-sm.mb-1
        span#progressText
      .w-full.bg-white.rounded-full.h-2
        .bg-blue-600.h-2.rounded-full.transition-all#progressBar(style="width: 0%")

    // Question display
    .question-container.bg-white.shadow-lg.rounded-xl.p-6.max-w-xl.w-full.mb-6
      p.question.text-xl.font-semibold.text-gray-800.mb-4 The question will appear here
      .options

    // Buttons
    .flex.gap-4
      button#nextQuestion(class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition") Next Question
      button#restartTest(class="bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-600 transition hidden") Restart Test

    // Footer info
    p#questionCounter.text-white.text-xl.font-semibold.mt-4 Question 1 of 7
    p#result.text-white.text-xl.font-semibold.mt-6
